<p> <%= review.rating %></p>
<P> <%= review.comment %></P>
<%if user_signed_in? and review.user_id==current_user.id %>
<%= link_to "Edit" , edit_book_review_path(review.book, review)%> |
<%= link_to "Delete", book_review_path(review.book, review), method: "delete", data: {confirm: "Are you sure you want to delete this review?"} %>
<% end %>